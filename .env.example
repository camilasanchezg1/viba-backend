# MongoDB connection string
MONGO_URL=mongodb+srv://<user>:<password>@cluster.mongodb.net/viba

# JWT
JWT_SECRET=your_super_secret_key_here
JWT_EXPIRES_IN=7d

# Server (Railway sets PORT automatically, no need to add it there)
PORT=3000
